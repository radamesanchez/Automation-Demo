package Pages;

import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;

/*public class signIn {
    WebElement signIn = wait.until(ExpectedConditions.elementToBeClickable(By.id("buttons")));
    WebElement inputContainer = wait.until(ExpectedConditions.elementToBeClickable(By.id("identifierId")));
    WebElement next = wait.until(ExpectedConditions.elementToBeClickable(By.id("identifierNext")));

    public void initializeSignIn() {

        signIn.click();
        inputContainer.sendKeys("radamesac.24@gmail.com");
        next.click();
    }
}
*/